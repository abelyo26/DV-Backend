/**
 *
 * generated schema for you {{ properCase modelName }} model
 *
 */

import mongoose from 'mongoose';

const {{ modelName }}Schema = new mongoose.Schema({
  // add your {{ modelName }} fields here
});

export default {{ modelName }}Schema;
